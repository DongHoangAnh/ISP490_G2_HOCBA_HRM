# Gói 4C — Tính công/lương OT theo hệ số + luật lễ/đêm Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Hệ số ca tự động theo luật lễ/đêm (lễ 3.0 / cuối tuần 2.0 / thường 1.5, +phụ cấp đêm); tổng hợp tháng của user có "Giờ OT" + "Công OT quy đổi" (Σ giờ ca approved × hệ số).

**Architecture:** Thêm policy fields lễ/đêm; nâng cấp `_default_rate` (model `hocba.work_shift`); thêm OT credit vào `_att_me_history` (controller); FE MyHistory 2 thẻ OT.

**Tech Stack:** Odoo 19 (Python), TransactionCase; React/Vite SPA.

**Spec:** [docs/superpowers/specs/2026-06-18-ot-credit-design.md](../specs/2026-06-18-ot-credit-design.md)

---

## Lệnh test & build

`MSYS_NO_PATHCONV=1` BẮT BUỘC. Xác nhận `0 failed, 0 error(s) of N tests`, N>0. Bash timeout 480000ms.
```bash
MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*' docker compose -f docker-compose.yml -f docker-compose.local.yml run --rm odoo \
  odoo -d hocba_hrm -u hocba_hrm,hocba_employees,hocba_attendance --addons-path=/mnt/extra-addons \
  --test-enable --test-tags /hocba_hrm --stop-after-init --log-level=test
# Build SPA: cd frontend && npm install && npm run build
```
Bỏ qua ERROR pre-existing `hb_timeoff_*`/`hr_holidays_modern`.

---

## Task 1: Policy — field lễ/đêm + view

**Files:**
- Modify: `custom-addons/hocba_attendance/models/hocba_attendance_policy.py`
- Modify: `custom-addons/hocba_attendance/views/hocba_attendance_policy_views.xml`

- [ ] **Step 1: Thêm 4 field.** Trong `hocba_attendance_policy.py`, sau field `shift_window_minutes`, thêm:
```python
    night_start = fields.Float(
        string='Giờ bắt đầu ca đêm', default=22.0,
        help='Ca có giờ vào ≥ giờ này (22.0 = 22:00) được tính phụ cấp đêm.')
    night_end = fields.Float(
        string='Giờ kết thúc ca đêm', default=6.0,
        help='...hoặc giờ vào < giờ này (6.0 = 06:00).')
    night_bonus = fields.Float(
        string='Phụ cấp ca đêm (+hệ số)', default=0.3,
        help='Cộng vào hệ số nếu ca rơi vào khung đêm (Luật LĐ +30%).')
    holiday_dates = fields.Text(
        string='Ngày lễ (YYYY-MM-DD)',
        help='Danh sách ngày lễ, phân tách bằng dấu phẩy hoặc xuống dòng. '
             'Ca rơi vào ngày lễ → hệ số nền 300%.')
```

- [ ] **Step 2: Thêm vào view.** Trong `views/hocba_attendance_policy_views.xml`, sau `<group string="Tính công">...</group>` (chứa `shift_window_minutes`), thêm group mới TRƯỚC `</sheet>`:
```xml
                        <group string="Hệ số ca (OT/lễ/đêm)">
                            <field name="night_start"/>
                            <field name="night_end"/>
                            <field name="night_bonus"/>
                            <field name="holiday_dates"/>
                        </group>
```

- [ ] **Step 3: Sync schema.** Run:
```bash
MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*' docker compose -f docker-compose.yml -f docker-compose.local.yml run --rm odoo \
  odoo -d hocba_hrm -u hocba_attendance --addons-path=/mnt/extra-addons --stop-after-init --log-level=warn
```
Expected: load sạch (không ParseError view, không lỗi field).

- [ ] **Step 4: Commit.**
```bash
git add custom-addons/hocba_attendance/models/hocba_attendance_policy.py custom-addons/hocba_attendance/views/hocba_attendance_policy_views.xml
git commit -m "feat(attendance): policy lễ/đêm (night_*, holiday_dates) (Gói 4C)"
```

---

## Task 2: `_default_rate` — luật lễ/đêm (TDD)

**Files:**
- Modify: `custom-addons/hocba_attendance/models/hocba_work_shift.py`
- Test: `custom-addons/hocba_hrm/tests/test_shift_api.py` (class `TestShiftApi` đã có)

- [ ] **Step 1: Viết test thất bại.** Thêm vào class `TestShiftApi` trong `custom-addons/hocba_hrm/tests/test_shift_api.py`:
```python
    def test_default_rate_weekday(self):
        WS = self.env['hocba.work_shift'].with_context(tz='Asia/Ho_Chi_Minh')
        # 2026-06-15 (T2) 09:00 local = 02:00 UTC
        from odoo import fields as _f
        dt = _f.Datetime.to_datetime('2026-06-15 02:00:00')
        self.assertEqual(WS._default_rate(dt), 1.5)

    def test_default_rate_weekend(self):
        WS = self.env['hocba.work_shift'].with_context(tz='Asia/Ho_Chi_Minh')
        from odoo import fields as _f
        dt = _f.Datetime.to_datetime('2026-06-20 02:00:00')  # T7 09:00 local
        self.assertEqual(WS._default_rate(dt), 2.0)

    def test_default_rate_holiday(self):
        self.env['hocba.attendance.policy'].get_policy().write(
            {'holiday_dates': '2026-06-15'})
        WS = self.env['hocba.work_shift'].with_context(tz='Asia/Ho_Chi_Minh')
        from odoo import fields as _f
        dt = _f.Datetime.to_datetime('2026-06-15 02:00:00')  # T2 nhưng là lễ
        self.assertEqual(WS._default_rate(dt), 3.0)

    def test_default_rate_night_weekday(self):
        self.env['hocba.attendance.policy'].get_policy().write(
            {'night_start': 22.0, 'night_end': 6.0, 'night_bonus': 0.3})
        WS = self.env['hocba.work_shift'].with_context(tz='Asia/Ho_Chi_Minh')
        from odoo import fields as _f
        # 2026-06-15 (T2) 23:00 local = 16:00 UTC -> 1.5 + 0.3
        dt = _f.Datetime.to_datetime('2026-06-15 16:00:00')
        self.assertEqual(WS._default_rate(dt), 1.8)
```
(`TestShiftApi.setUp` đã có; các test này độc lập, tự set policy khi cần.)

- [ ] **Step 2: Chạy test — FAIL** (giá trị cũ: weekday cũ trả 1.5 nên `test_default_rate_weekday` có thể PASS sẵn; nhưng `test_default_rate_holiday`/`test_default_rate_night_weekday` FAIL vì logic lễ/đêm chưa có). Run lệnh test. Confirm N>0 và có test fail đúng như mong đợi (holiday trả 1.5 thay vì 3.0; night trả 1.5 thay vì 1.8).

- [ ] **Step 3: Implement.** Trong `custom-addons/hocba_attendance/models/hocba_work_shift.py`, thay TOÀN BỘ method `_default_rate` bằng:
```python
    @api.model
    def _default_rate(self, start_dt):
        """Hệ số gợi ý: lễ=3.0 / cuối tuần=2.0 / ngày thường=1.5; cộng phụ cấp đêm
        nếu ca rơi khung đêm. Lễ lấy từ policy.holiday_dates (YYYY-MM-DD, phân tách
        dấu phẩy/xuống dòng). start_dt là Datetime UTC naive."""
        if not start_dt:
            return 1.0
        policy = self.env['hocba.attendance.policy'].sudo().get_policy()
        local = fields.Datetime.context_timestamp(self, start_dt)
        holidays = set((policy.holiday_dates or '').replace(',', ' ').split())
        if local.strftime('%Y-%m-%d') in holidays:
            base = 3.0
        elif local.weekday() >= 5:
            base = 2.0
        else:
            base = 1.5
        hour = local.hour + local.minute / 60.0
        if hour >= (policy.night_start or 22.0) or hour < (policy.night_end or 6.0):
            base += (policy.night_bonus or 0.0)
        return base
```
`fields`, `api` đã import sẵn ở đầu file. Giữ chữ ký `@api.model _default_rate(self, start_dt)`.

- [ ] **Step 4: Chạy test — 4 test mới PASS + suite cũ xanh** (gồm test 4A `test_create_pins_employee_and_default_rate`=1.5, `test_create_weekend_rate`=2.0 — vẫn đúng vì ca 09:00 không phải đêm). Run lệnh test. Expected `0 failed, 0 error(s) of N tests`, N>0.

- [ ] **Step 5: Commit.**
```bash
git add custom-addons/hocba_attendance/models/hocba_work_shift.py custom-addons/hocba_hrm/tests/test_shift_api.py
git commit -m "feat(attendance): _default_rate luật lễ/đêm (Gói 4C)"
```

---

## Task 3: `_att_me_history` — quy đổi công OT (TDD)

**Files:**
- Modify: `custom-addons/hocba_hrm/controllers/main.py`
- Test: `custom-addons/hocba_hrm/tests/test_ot_credit.py` (tạo mới)
- Modify: `custom-addons/hocba_hrm/tests/__init__.py`

- [ ] **Step 1: Đăng ký test module.** Trong `custom-addons/hocba_hrm/tests/__init__.py` thêm:
```python
from . import test_ot_credit
```

- [ ] **Step 2: Viết test thất bại.** Create `custom-addons/hocba_hrm/tests/test_ot_credit.py`:
```python
from odoo import fields
from odoo.tests.common import TransactionCase
from odoo.tests import tagged

from odoo.addons.hocba_hrm.controllers.main import _att_me_history


@tagged('post_install', '-at_install')
class TestOtCredit(TransactionCase):

    def setUp(self):
        super().setUp()
        self.emp = self.env['hr.employee'].create({
            'name': 'CTV OT', 'x_employment_status': 'ctv'})
        self.user = self.env['res.users'].create({
            'name': 'CTV OT User', 'login': 'ctv_ot_user'})
        self.user.tz = 'Asia/Ho_Chi_Minh'
        self.emp.user_id = self.user
        self.WS = self.env['hocba.work_shift'].with_context(tz='Asia/Ho_Chi_Minh')

    def _shift(self, start, end, rate, state='approved'):
        return self.WS.sudo().create({
            'employee_id': self.emp.id, 'start': start, 'end': end,
            'shift_type': 'ot', 'rate': rate, 'state': state})

    def test_ot_summary(self):
        # 2h ×1.5 + 3h ×2.0 trong tháng 6/2026
        self._shift('2026-06-15 02:00:00', '2026-06-15 04:00:00', 1.5)  # 2h
        self._shift('2026-06-16 02:00:00', '2026-06-16 05:00:00', 2.0)  # 3h
        s = _att_me_history(self.env(user=self.user), '2026-06')['summary']
        self.assertEqual(s['otShiftCount'], 2)
        self.assertEqual(s['otHours'], 5.0)
        self.assertEqual(s['otCreditHours'], 9.0)   # 2*1.5 + 3*2.0

    def test_ot_other_month_excluded(self):
        self._shift('2026-07-15 02:00:00', '2026-07-15 04:00:00', 1.5)  # tháng 7
        s = _att_me_history(self.env(user=self.user), '2026-06')['summary']
        self.assertEqual(s['otShiftCount'], 0)
        self.assertEqual(s['otHours'], 0)

    def test_ot_pending_excluded(self):
        self._shift('2026-06-15 02:00:00', '2026-06-15 04:00:00', 1.5, state='pending')
        s = _att_me_history(self.env(user=self.user), '2026-06')['summary']
        self.assertEqual(s['otShiftCount'], 0)
```

- [ ] **Step 3: Chạy test — FAIL** (`KeyError: 'otShiftCount'`). Run lệnh test. Confirm N>0.

- [ ] **Step 4: Implement.** Trong `custom-addons/hocba_hrm/controllers/main.py`, hàm `_att_me_history(env, month_str)`. Tìm chỗ dựng `summary` dict (kết thúc bằng `}` trước `return {'month': ..., 'summary': summary, 'rows': rows}`). NGAY TRƯỚC dòng `return {...}`, thêm:
```python
    shifts = env['hocba.work_shift'].sudo().search([
        ('employee_id', '=', emp.id), ('state', '=', 'approved')])
    month_shifts = shifts.filtered(
        lambda s: first <= fields.Datetime.context_timestamp(s, s.start).date() <= last)
    ot_hours = sum((s.end - s.start).total_seconds() / 3600.0 for s in month_shifts)
    ot_credit = sum(
        (s.end - s.start).total_seconds() / 3600.0 * s.rate for s in month_shifts)
    summary['otShiftCount'] = len(month_shifts)
    summary['otHours'] = round(ot_hours, 2)
    summary['otCreditHours'] = round(ot_credit, 2)
```
`emp`, `first`, `last`, `summary` đã có sẵn trong `_att_me_history` (đọc lại hàm xác nhận tên biến: `emp = env.user.employee_id`, `first = date(y, m, 1)`, `last = date(y, m, calendar.monthrange(...)[1])`, `summary = {...}`). `fields` đã import. KHÔNG đổi phần khác.

- [ ] **Step 5: Chạy test — 3 test PASS + suite xanh.** Run lệnh test. Expected `0 failed, 0 error(s) of N tests`, N>0.

- [ ] **Step 6: Commit.**
```bash
git add custom-addons/hocba_hrm/controllers/main.py custom-addons/hocba_hrm/tests/test_ot_credit.py custom-addons/hocba_hrm/tests/__init__.py
git commit -m "feat(attendance-api): _att_me_history quy đổi công OT theo hệ số (Gói 4C)"
```

---

## Task 4: Frontend — thẻ OT trong MyHistory + build

**Files:**
- Modify: `frontend/src/features/attendance/MyHistory.jsx`

- [ ] **Step 1: READ** `frontend/src/features/attendance/MyHistory.jsx`.

- [ ] **Step 2: Thêm 2 thẻ OT + đổi lưới.** Trong khối summary, đổi:
```jsx
          <div className="stat-grid" style={{ gridTemplateColumns: 'repeat(4,1fr)', marginBottom: 16 }}>
            <Sum val={data.summary.daysPresent} lbl="Ngày có mặt" />
            <Sum val={data.summary.totalCredit} lbl="Tổng công" />
            <Sum val={data.summary.deficitCredit} lbl="Công thiếu" col={data.summary.deficitCredit > 0 ? 'var(--amber)' : undefined} />
            <Sum val={data.summary.netCredit} lbl="Công thực" col="var(--green)" />
          </div>
```
thành:
```jsx
          <div className="stat-grid" style={{ gridTemplateColumns: 'repeat(6,1fr)', marginBottom: 16 }}>
            <Sum val={data.summary.daysPresent} lbl="Ngày có mặt" />
            <Sum val={data.summary.totalCredit} lbl="Tổng công" />
            <Sum val={data.summary.deficitCredit} lbl="Công thiếu" col={data.summary.deficitCredit > 0 ? 'var(--amber)' : undefined} />
            <Sum val={data.summary.netCredit} lbl="Công thực" col="var(--green)" />
            <Sum val={data.summary.otHours} lbl="Giờ OT" />
            <Sum val={data.summary.otCreditHours} lbl="Công OT quy đổi" col="var(--green)" />
          </div>
```
(Chỉ đổi block này; phần bảng + drawer giữ nguyên.)

- [ ] **Step 3: Build SPA.** Run:
```bash
cd frontend && npm install && npm run build
```
Expected: build thành công, output → `custom-addons/hocba_hrm/static/spa`. Sửa nếu lỗi rồi build lại.

- [ ] **Step 4: Commit (gồm build).**
```bash
git add frontend/src/features/attendance/MyHistory.jsx custom-addons/hocba_hrm/static/spa
git commit -m "feat(shift-ui): MyHistory hiển thị Giờ OT + Công OT quy đổi + build (Gói 4C)"
```

---

## Task 5: Kiểm thử cuối + handoff + merge

- [ ] **Step 1: Chạy lại toàn bộ test backend — xanh, N>0.** Run lệnh test. Expected `0 failed, 0 error(s) of N tests`, N>0.

- [ ] **Step 2: Kiểm thử thủ công SPA (spec §5).** MyHistory tháng có ca OT approved → "Giờ OT"/"Công OT quy đổi" đúng.

- [ ] **Step 3: Cập nhật handoff.** Modify `docs/superpowers/HANDOFF-attendance-upgrade.md`: bảng §1 Gói 4C → ✅ XONG; §3 ghi 4C hoàn tất + link spec/plan. **Toàn bộ handoff (Gói 1-4) hoàn tất.** Commit:
```bash
git add docs/superpowers/HANDOFF-attendance-upgrade.md
git commit -m "docs(attendance): Gói 4C hoàn tất — toàn bộ handoff nâng cấp chấm công xong"
```

- [ ] **Step 4: Merge về `main`** (skill `superpowers:finishing-a-development-branch`). Trước merge: fetch + merge `origin/main` mới nhất (có thể có commit thành viên khác), chạy lại test + build xác nhận xanh, rồi push.

---

## Self-Review (đối chiếu spec)
- §1 Policy lễ/đêm + view: Task 1. ✅
- §2 `_default_rate` luật lễ/đêm: Task 2 (4 test: weekday/weekend/holiday/night). ✅
- §3 `_att_me_history` OT credit: Task 3 (3 test: summary/other-month/pending). ✅
- §4 FE MyHistory 2 thẻ OT: Task 4. ✅
- §5/§6 Test + phạm vi: không payroll/model-lễ-riêng/_do_check. ✅

**Type consistency:** `_default_rate(self, start_dt)` chữ ký không đổi (4A `_shift_create`/`_shift_decide` gọi vẫn đúng). `summary` keys mới `otShiftCount`/`otHours`/`otCreditHours` khớp `_att_me_history`↔test↔FE (`data.summary.otHours`/`otCreditHours`). Policy fields `night_start`/`night_end`/`night_bonus`/`holiday_dates` khớp model↔`_default_rate`↔view.
