[options]
; Dev local: Postgres trong Docker không bật SSL (odoo.conf gốc require cho Neon)
db_sslmode = prefer
; Thông tin kết nối DB được truyền từ .env qua Docker command.
proxy_mode = True
list_db = False
